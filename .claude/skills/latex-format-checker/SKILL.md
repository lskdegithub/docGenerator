---
name: latex-format-checker
description: Check LaTeX .tex files for compliance with technical document formatting standards. Validates line spacing (18pt), font sizes, title formats, table spacing, and Chinese character handling. Use when user asks to check, validate, or verify LaTeX document formatting, or mentions format standards, GJB 438C-2021 compliance, or technical document requirements.
allowed-tools: Read, Grep, Glob
---

# LaTeX Format Checker

检查 LaTeX 文件是否符合技术文档格式标准。

## 检查项目

### 1. 全局设置检查
- ✓ 文档类声明是否为 `\documentclass[12pt,a4paper]{article}`
- ✓ 是否加载 ctex 宏包支持中文
- ✓ 是否使用 fontspec 设置字体
- ✓ 行间距设置：`\baselinestretch` 应为 1.71（18磅）

### 2. 字号定义检查
- ✓ 是否定义了 `\wuhao` (五号, 10.5pt)
- ✓ 是否定义了 `\xiaowu` (小五号, 9pt)
- ✓ 是否定义了 `\sanhao` (三号, 16pt)

### 3. 标题格式检查
- ✓ 标题是否使用 `\titlespacing*` 设置间距
- ✓ 标题段前段后间距应为 6pt
- ✓ 标题字体应为五号宋体加粗

### 4. 表格格式检查
- ✓ 表格题注是否为五号黑体居中
- ✓ 表格是否使用 `\settablespacing` 设置行间距
- ✓ 表格内容是否使用小五号字体
- ✓ 表头是否使用灰色背景 `\rowcolor{gray!20}`

### 5. 页面设置检查
- ✓ 页边距：上下左右各 2.5cm
- ✓ 段落首行缩进：1.5em
- ✓ 页码格式：小五号新罗马体，居中

## 使用方法

当用户要求检查格式时，按以下步骤操作：

1. **读取 .tex 文件**
   ```bash
   # 使用 Read 工具读取文件内容
   ```

2. **逐项检查**
   - 使用 Grep 搜索关键设置
   - 验证数值是否符合标准
   - 记录不符合项

3. **生成报告**
   - 列出所有不符合标准的项
   - 提供修正建议
   - 标注问题所在行号

## 常见问题示例

### 问题1：行间距不正确
```
❌ 错误：\renewcommand{\baselinestretch}{1.5}
✓ 正确：\renewcommand{\baselinestretch}{1.71}
```

### 问题2：标题间距不正确
```
❌ 错误：\titlespacing{\section}{0pt}{6pt}{6pt}
✓ 正确：\titlespacing*{\section}{0pt}{6pt}{0pt}
```

### 问题3：缺少中文支持
```
❌ 缺少：\usepackage{ctex}
✓ 正确：应包含此宏包
```

## 检查命令示例

```bash
# 检查行间距设置
grep "baselinestretch" filename.tex

# 检查标题格式
grep "titlespacing" filename.tex

# 检查表格设置
grep "arraystretch" filename.tex
```

## 输出格式

检查结果以 Markdown 格式输出：

```markdown
# LaTeX 格式检查报告

## ✅ 符合标准的项目
- 行间距设置：18磅 (1.71倍)
- 标题格式：五号宋体加粗

## ❌ 不符合标准的项目
- [行号] 标题间距设置不正确
  - 当前值：{before: 6pt, after: 6pt}
  - 应为值：{before: 6pt, after: 0pt}
  - 修正建议：使用 \titlespacing* 命令

## 📊 统计信息
- 检查项总数：20
- 符合标准：18
- 需要修正：2
```

## 注意事项

1. 此 Skill 仅检查格式定义，不检查内容正确性
2. 编译错误不在检查范围内
3. 严格遵循 GJB 438C-2021 技术文档格式标准
4. 检查结果仅供参考，最终以实际输出为准
