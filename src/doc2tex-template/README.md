# LaTeX 测试文档 - 章节拆分版本

## 目录结构

```
src/doc2tex-template/
├── gjb438c-style.sty          # 📌 统一样式文件（所有文档共用）
├── README.md                  # 📖 使用说明
│
├── test_plan/                 # 测试大纲（8章）
│   ├── main.tex              # 主文件
│   └── chapters/             # 📁 章节文件夹
│       ├── chapter1.tex       # 第1章：范围
│       ├── chapter2.tex       # 第2章：引用文档
│       ├── chapter3.tex       # 第3章：软件测试环境
│       ├── chapter4.tex       # 第4章：测试说明
│       ├── chapter5.tex       # 第5章：测试进度
│       ├── chapter6.tex       # 第6章：测试终止条件
│       ├── chapter7.tex       # 第7章：需求的可追踪性
│       └── chapter8.tex       # 第8章：注释
│
├── test_detail/               # 测试细则（5章+附录）
│   ├── main.tex              # 主文件
│   └── chapters/             # 📁 章节文件夹
│       ├── chapter1.tex       # 第1章：范围
│       ├── chapter2.tex       # 第2章：引用文档
│       ├── chapter3.tex       # 第3章：测试准备
│       ├── chapter4.tex       # 第4章：测试说明
│       ├── chapter5.tex       # 第5章：需求的可追踪性
│       └── chapterA.tex       # 附录
│
└── test_report/               # 测试报告（4章+附录）
    ├── main.tex              # 主文件
    └── chapters/             # 📁 章节文件夹
        ├── chapter1.tex       # 第1章：范围
        ├── chapter2.tex       # 第2章：引用文档
        ├── chapter3.tex       # 第3章：测试结果概述
        ├── chapter4.tex       # 第4章：详细测试结果
        └── chapterA.tex       # 附录
```

## 特点

✅ **章节拆分**：每个文档按章节独立成多个 .tex 文件  
✅ **目录清晰**：章节文件统一放在 chapters/ 文件夹中  
✅ **多人协作**：不同人员可以同时编辑不同章节  
✅ **格式统一**：所有文档共用 gjb438c-style.sty 样式文件  
✅ **易于维护**：修改格式只需改样式文件，所有文档自动生效  
✅ **结构简洁**：每个文档目录只有 main.tex 和 chapters/ 两个元素  

## 编译方法

### 编译所有文档
```bash
./build.sh
```

### 只编译某个文档
```bash
./build.sh test_plan     # 只编译测试大纲
./build.sh test_detail   # 只编译测试细则
./build.sh test_report   # 只编译测试报告
```

## 协作流程

1. **分配章节**：将章节文件分配给不同人员
   - 例如：A负责 `chapters/chapter1.tex`，B负责 `chapters/chapter2.tex`

2. **独立编辑**：各人在自己的分支上编辑对应章节文件

3. **合并代码**：通过 Git 合并各人的修改

4. **编译验证**：运行 `./build.sh` 编译所有文档

5. **生成PDF**：编译后的PDF文件位于 `output/` 目录

## 注意事项

- ⚠️ 不要修改 `main.tex` 中的 `\input` 顺序
- ⚠️ 编辑章节文件时，确保 `\section*` 等标题格式正确
- ⚠️ 所有章节文件不需要 `\documentclass`、`\usepackage` 等导言区内容
- ✅ 每个章节文件只包含该章节的内容即可
- ✅ 章节文件统一放在 `chapters/` 文件夹中

## 文件说明

- `main.tex`：主文件，通过 `\input{chapters/chapterX.tex}` 包含各个章节
- `chapters/chapterX.tex`：章节文件，包含该章节的所有内容
- `gjb438c-style.sty`：统一样式文件，定义字号、间距、标题、表格等格式

## 目录结构优点

- 📁 **组织清晰**：章节文件统一管理，一目了然
- 🔍 **查找快速**：需要哪个章节直接进入 chapters/ 文件夹
- 📦 **易于打包**：可以单独打包某个文档的 chapters/ 文件夹
- 🎯 **职责分明**：main.tex 负责组织，chapters/ 负责内容
